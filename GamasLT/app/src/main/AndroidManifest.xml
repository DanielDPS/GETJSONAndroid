<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dany.gamaslt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:configChanges="screenSize|orientation"
            android:name=".OrdenesDeServicio"

            android:label="Ordenes de Servicio"></activity>
        <activity
            android:configChanges="screenSize|orientation"
            android:name=".DetalleOrden"
            android:label="Detalle de Orden"
            android:parentActivityName=".OrdenesDeServicio"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>